{
  "name": "ask-about-alice",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker backing the Ask about Alice assistant.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
